const LibraryPage: React.FC = () => {
    return (
        <section className="explore-dashboard">
            <div className="explore-content">
                <div className="explore-feedback-card">
                    <h3>Library is still being built</h3>
                    <p>
                        The dashboard shell is ready. Next step is wiring saved games and
                        user-owned collections into this view.
                    </p>
                </div>
            </div>
        </section>
    );
};

export default LibraryPage;
